{
  "name": "replicated-log",
  "version": "0.0.0",
  "private": true,
  "author": "Tariod <mykhailovdmt@gmail.com>",
  "homepage": "https://github.com/Tariod/replicated-log#readme",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Tariod/replicated-log.git"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "prebuild": "lerna run bootstrap",
    "build": "lerna run build",
    "version": "lerna version",
    "start": "lerna run start",
    "start:dev": "lerna run start:dev",
    "start:prod": "lerna run start:prod",
    "lint": "lerna run lint",
    "fmt": "lerna run fmt"
  },
  "devDependencies": {
    "lerna": "^5.5.4"
  }
}
